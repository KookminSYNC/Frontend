Always use tailwindcss for all styling needs.
Always respond in Korean
Never use gradient color styling in UI. Do not add Tailwind gradient utilities (`bg-gradient-*`, `from-*`, `via-*`, `to-*`) or CSS gradient backgrounds/text. Use flat solid colors only.
